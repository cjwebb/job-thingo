<h2>Welcome to JobThing</h2>
<p>Yes, hello world! You must be looking for a software job if you've stumbled upon this site.</p>

<div class="panel panel-primary">
  <div class="panel-heading">
    <h3 class="panel-title">Post New Job Description:</h3>
  </div>
  <div class="panel-body">
    <form role="form" method="POST" action="/gen-job">
      <div class="form-group">
        <label for="title">Title</label>
        <input type="text" class="form-control" name="title" placeholder="Job Title (e.g. Java Software Engineer)" value="{{.Title}}">
      </div>
      <div class="form-group">
        <label for="description">Description</label>
        <textarea rows="8" class="form-control" name="description" placeholder="Enter a description here. Try to include as much detail as possible. Basic HTML is allowed." value="{{.Description}}"></textarea>
	<p class="help-block">Basic HTML is allowed</p>
      <div class="form-group">
        <label for="rate">Rate</label>
        <input type="text" class="form-control" name="rate" placeholder="Rate (e.g. £50000/year or £500/day)" value="{{.Rate}}">
      </div>
      <div class="form-group">
        <label for="contact_email">Contact Email</label>
        <input type="email" class="form-control" name="contact_email" placeholder="Who should the applicant email to apply?" value="{{.ContactEmail}}">
      </div>
      <div class="form-group">
        <label for="email">Your Email address</label>
        <input type="email" class="form-control" name="user_email" placeholder="Enter your email" value="{{.UserEmail}}">
        <p class="help-block">We won't send you any emails unless your referral is successful. In that case, we will need to contact you to pay any referral fees due.</p>
      </div>
      <button type="submit" class="btn btn-default">Submit</button>
    </form>
  </div>
</div>

